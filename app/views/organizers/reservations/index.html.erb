<div class="container">
  <h2>予約イベント</h2>
  <table class="table">
    <thead>
        <tr>
        <th></th>
        <th>イベント名</th>
        <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
        <% @reservations.each do |r| %>
        <tr>
            <td><%= attachment_image_tag e, :image , fallback: "no_image.jpeg", size: '40x40'%></td>
            <td><%= e.event_name %></td>
            <td><%= e.content %></td>
            <td><%= l e.date_and_time %></td>
            <td><%= link_to "イベント詳細", event_path(@event) %></td>
        </tr>
        <% end %>
    </tbody>
  </table>
</div>