<div class="col-md-6 offset-4 mt-4">
<h4>下記の必須項目を登録していただくと虫めがね村の住人になれます。</h4>
<div class="col-xs-12">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "participants/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :nickname %><br />
    <%= f.text_field :nickname, :size=>"40", autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :telephone_number %>（ハイフンなし）<br />
    <%= f.text_field :telephone_number, :size=>"40",autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, :size=>"40", autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> 文字以上で入力してください)</em>
    <% end %><br />
    <%= f.password_field :password, :size=>"40", autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, :size=>"40", autofocus: true %>
  </div>

  <div class="field">
    <%= f.check_box :agreement, :as => :boolean, checked:false %><%= link_to "利用規約とプライバシーポリシー", contract_path, class: "link-about" %>を受諾する
  </div>

  <div class="actions">
    <%= f.submit "虫めがね村の住人になる", class: "btn btn-info btn-md mt-3" %>
  </div>
<% end %>

<%= render "participants/shared/links" %>
</div>
</div>